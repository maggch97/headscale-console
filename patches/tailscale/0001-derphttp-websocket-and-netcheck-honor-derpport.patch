diff --git a/derp/derphttp/derphttp_client.go b/derp/derphttp/derphttp_client.go
index 3c8408e95..927022c6f 100644
--- a/derp/derphttp/derphttp_client.go
+++ b/derp/derphttp/derphttp_client.go
@@ -280,7 +280,14 @@ func (c *Client) urlString(node *tailcfg.DERPNode) string {
 	if debugUseDERPHTTP() {
 		proto = "http"
 	}
-	return fmt.Sprintf("%s://%s/derp", proto, node.HostName)
+	return fmt.Sprintf("%s://%s/derp", proto, derpNodeURLHost(node))
+}
+
+func derpNodeURLHost(node *tailcfg.DERPNode) string {
+	if node.DERPPort == 0 {
+		return node.HostName
+	}
+	return net.JoinHostPort(node.HostName, fmt.Sprint(node.DERPPort))
 }
 
 // AddressFamilySelector decides whether IPv6 is preferred for
@@ -857,7 +864,14 @@ func (c *Client) dialNodeUsingProxy(ctx context.Context, n *tailcfg.DERPNode, pr
 		}
 	}()
 
-	target := net.JoinHostPort(n.HostName, "443")
+	port := "443"
+	if !c.useHTTPS() {
+		port = "3340"
+	}
+	if n.DERPPort != 0 {
+		port = fmt.Sprint(n.DERPPort)
+	}
+	target := net.JoinHostPort(n.HostName, port)
 
 	var authHeader string
 	if v, err := tshttpproxy.GetAuthHeader(pu); err != nil {
diff --git a/net/netcheck/netcheck.go b/net/netcheck/netcheck.go
index 8ae17646d..989cb5280 100644
--- a/net/netcheck/netcheck.go
+++ b/net/netcheck/netcheck.go
@@ -82,6 +82,17 @@ const (
 	defaultInitialRetransmitTime = 100 * time.Millisecond
 )
 
+func derpProbeURL(node *tailcfg.DERPNode) string {
+	return "https://" + derpNodeURLHost(node) + "/derp/probe"
+}
+
+func derpNodeURLHost(node *tailcfg.DERPNode) string {
+	if node.DERPPort == 0 {
+		return node.HostName
+	}
+	return net.JoinHostPort(node.HostName, fmt.Sprint(node.DERPPort))
+}
+
 // Report contains the result of a single netcheck.
 type Report struct {
 	Now         time.Time // the time the report was run
@@ -1072,7 +1083,7 @@ func (c *Client) measureHTTPSLatency(ctx context.Context, dm *tailcfg.DERPMap, r
 		go func() {
 			defer wg.Done()
 			node := rg.Nodes[0]
-			req, _ := http.NewRequestWithContext(ctx, "HEAD", "https://"+node.HostName+"/derp/probe", nil)
+			req, _ := http.NewRequestWithContext(ctx, "HEAD", derpProbeURL(node), nil)
 			// One warm-up one to get HTTP connection set
 			// up and get a connection from the browser's
 			// pool.
